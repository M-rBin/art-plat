# 开发环境 - 只启动 MySQL + Redis，后端本地运行
services:
  mysql:
    container_name: ByAgent-mysql-dev
    image: mysql:8.0
    restart: unless-stopped
    environment:
      TZ: "Asia/Shanghai"
      MYSQL_ROOT_PASSWORD: "123456"
      MYSQL_DATABASE: "ByAgent"
    ports:
      - "127.0.0.1:3307:3306"
    volumes:
      - ./data/mysql:/var/lib/mysql
    networks:
      - ByAgent-dev

  redis:
    container_name: ByAgent-redis-dev
    image: redis:7-alpine
    restart: unless-stopped
    environment:
      TZ: "Asia/Shanghai"
    ports:
      - "127.0.0.1:6380:6379"
    volumes:
      - ./data/redis:/data
    networks:
      - ByAgent-dev

networks:
  ByAgent-dev:
    driver: bridge
